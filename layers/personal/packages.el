;;; packages.el --- personal layer packages file -*- lexical-binding: t -*-
;;
;; Extra packages installed via this layer.  Package-specific configuration
;; lives in the topic modules under ./modules/ and is loaded by config.el.

(defconst personal-packages
  '()
  "The list of Lisp packages required by the personal layer.")

;;; packages.el ends here
